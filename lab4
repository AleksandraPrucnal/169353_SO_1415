do pliku dane22 zapisać informację o pogodzie dopisać: wynik polecenia ls –l, następnie dopisać nazwę bieżącego katalogu
echo info o pogodzie > dane22.txt
ls -l >> dane22.txt

do pliku drzewo zapisać strukturę katalogu domowego, następnie pliki dane 22 i drzewo zapisać do pliku wynik;
tree ~  > drzewo.txt
cat dane22.txt > wynik.txt
cat drzewo.txt  >> wynik.txt

wykorzystując mechanizm WE zapisać do jednego pliku razem zawartość plików dane22, drzewo, wynik;
cat drzewo.txt  >> wynik.txt

policz ilu użytkowników pracuje teraz na serwerze;
who |wc -l

policz, ile plików i podkatalogów jest w danym katalogu
ls | wc -l


wszystkie puste pliki zwykłe w katalogu domowym i jego podkatalogach do poziomu 1;
find ~ -maxdepth 1 -type f -empty
